Create a 3.10 python virtual environment and run the following:
pip install --upgrade setuptools wheel ninja
pip install torch==2.0.0
pip install torch_lfilter==0.0.3 -i https://pypi.org/simple
pip install -r requirements.txt
